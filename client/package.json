{
  "name": "@hive/client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(lint placeholder)\"",
    "bundle:report": "vite build --mode production && node scripts/bundle-size.mjs"
  },
  "dependencies": {
    "@hive/protocol": "workspace:*",
    "@hive/shared": "workspace:*",
    "phaser": "^3.85.2",
    "colyseus.js": "^0.16.6"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
